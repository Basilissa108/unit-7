import React from "react";
import "./spinner.css";

// assign functional component to const Spinner
const Spinner = () => { return <div className='spinner' /> };

// export Spinner component as default
export default Spinner;